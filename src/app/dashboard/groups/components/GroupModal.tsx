import { FullGroupType } from "@/types";

interface GroupModalProps {
  group: FullGroupType;
}

const GroupModal = ({}: GroupModalProps) => {
  return <div className="">GroupModal</div>;
};

export default GroupModal;
